[build]
  builder = "DOCKERFILE"

[deploy]
  startCommand = "java -jar app.jar"
  healthcheckPath = "/api/v1/groups"
  healthcheckInterval = 30
  restartPolicyType = "ON_FAILURE"
  restartPolicyMaxRetries = 10

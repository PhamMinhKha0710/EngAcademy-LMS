# =================== Default Properties ===================
# These provide hardcoded fallbacks so the app starts even if the
# prod profile is not activated and/or env vars are not set.

# JWT Security - hardcoded defaults (must be Base64-encoded 32-byte keys)
application.security.jwt.secret-key=404E635266556A586E3272357538782F413F4428472B4B6250645367566B5970
application.security.jwt.expiration=86400000
application.security.jwt.refresh-token.expiration=604800000

# CORS - permissive defaults for development
application.cors.allowed-origins=http://localhost:3000,http://localhost:3001

# Rate Limiting - defaults
rate-limit.max-attempts=100
rate-limit.window-seconds=60
rate-limit.enabled=true

# Redis - localhost defaults (safe fallback)
spring.data.redis.host=localhost
spring.data.redis.port=6379
spring.data.redis.username=
spring.data.redis.password=

# Mail - disabled defaults
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=
spring.mail.password=
spring.mail.default-encoding=UTF-8

# Flyway disabled by default
spring.flyway.enabled=false

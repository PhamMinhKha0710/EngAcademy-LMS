package com.englishlearn.domain.enums;

/**
 * Nhóm badge theo chủ đề - dùng để phân loại và filter badge.
 */
public enum BadgeGroup {
    STREAK,    // Chuỗi ngày học
    LESSON,    // Bài học & từ vựng
    QUIZ,      // Bài kiểm tra
    LEVEL,     // Cấp độ & tiến độ
    SPECIAL,   // Thời điểm đặc biệt (ngày lễ, sinh nhật...)
    SOCIAL     // Xã hội & thách thức
}
